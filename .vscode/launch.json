
{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug Streamlit with UV",
            "type": "debugpy",
            "request": "launch",
            "module":"streamlit",
            "args": ["run", "app/chat_app.py"],
            "env": {
                "PYTHONPATH": "${workspaceFolder}"
            },
            "python": "${workspaceFolder}/.venv/bin/python"
        }
    ]
}
